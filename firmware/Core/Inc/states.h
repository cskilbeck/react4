// for debugging
STATE(test)

// boot anim
STATE(startup)

// attract mode
STATE(waiting)

// game starting
STATE(game_start)

// turn starting (random delay)
STATE(turn_begins)

// light goes on
STATE(snap)

// somebody won a point
STATE(win)

// somebody jumped the gun
STATE(lose)

// somebody won the game
STATE(game_over)
